@page "/mod/{Id:int}"
@using OpenMods.Server.Models

<PageTitle>@(_mod?.Name ?? "Loading...") | OpenMods</PageTitle>

@if (_mod == null)
{
    <div class="flex items-center justify-center min-h-[60vh]">
        <div class="animate-pulse text-slate-400">Loading mod details...</div>
    </div>
}
else
{
    <main class="px-10 py-6 max-w-[1400px] mx-auto w-full">
        <nav class="flex items-center gap-2 mb-8 text-[#92a4c9] text-sm font-display">
            <a class="hover:text-white transition-colors" href="/">OpenMods</a>
            <span class="material-symbols-outlined text-sm">chevron_right</span>
            <a class="hover:text-white transition-colors" href="explore">Explore</a>
            <span class="material-symbols-outlined text-sm">chevron_right</span>
            <span class="text-white">@_mod.Name</span>
        </nav>

        <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-6 mb-10 pb-8 border-b border-border-dark font-display">
            <div class="flex gap-6 items-center">
                <div class="size-24 rounded-xl bg-card-dark border border-border-dark flex items-center justify-center overflow-hidden">
                    <div class="w-full h-full bg-gradient-to-br from-primary/20 to-primary/40 flex items-center justify-center">
                        <span class="material-symbols-outlined text-5xl text-primary">auto_awesome</span>
                    </div>
                </div>
                <div>
                    <div class="flex items-center gap-3">
                        <h1 class="text-3xl font-bold tracking-tight">@_mod.Name</h1>
                        <span class="px-2 py-0.5 bg-primary/20 text-primary rounded text-xs font-bold uppercase tracking-wider">@LatestVersion</span>
                    </div>
                    <div class="flex items-center gap-4 mt-2 text-[#92a4c9]">
                        <div class="flex items-center gap-2">
                            <div class="size-6 rounded-full bg-cover" style="background-image: url('@_mod.Developer?.AvatarUrl')"></div>
                            <span class="text-sm">by <span class="text-white font-medium">@_mod.Developer?.DisplayName</span></span>
                        </div>
                        <span class="text-border-dark">|</span>
                        <a class="flex items-center gap-1.5 hover:text-white transition-colors" href="@_mod.GitHubRepoUrl" target="_blank">
                            <span class="material-symbols-outlined text-sm">link</span>
                            <span class="text-sm">@_mod.GitHubRepoUrl?.Replace("https://", "")</span>
                        </a>
                    </div>
                </div>
            </div>
            <div class="flex gap-3">
                <button class="flex items-center gap-2 px-5 py-2.5 rounded-lg border border-border-dark bg-card-dark text-sm font-bold hover:bg-border-dark transition-colors">
                    <span class="material-symbols-outlined text-[18px]">star</span> Star @FormatCount(_mod.Stars)
                </button>
                <button class="flex items-center gap-2 px-5 py-2.5 rounded-lg border border-border-dark bg-card-dark text-sm font-bold hover:bg-border-dark transition-colors">
                    <span class="material-symbols-outlined text-[18px]">fork_right</span> Fork @FormatCount(_mod.Forks)
                </button>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-12 gap-10">
            <div class="lg:col-span-8">
                <div class="bg-card-dark/30 rounded-xl border border-border-dark p-8 markdown-body font-body text-slate-300">
                    <div class="flex items-center gap-3 mb-8 text-[#92a4c9] pb-4 border-b border-border-dark font-display">
                        <span class="material-symbols-outlined text-[20px]">description</span>
                        <span class="text-sm font-medium">README.md</span>
                    </div>
                    
                    <div class="prose prose-invert max-w-none">
                        @((MarkupString)_mod.LongDescription!)
                    </div>
                </div>
            </div>

            <div class="lg:col-span-4 space-y-6 font-display">
                <div class="bg-card-dark border border-border-dark rounded-xl p-6">
                    <button class="w-full flex items-center justify-center gap-3 bg-primary hover:bg-primary/90 py-4 rounded-lg font-bold text-white transition-all mb-4 shadow-lg shadow-primary/20">
                        <span class="material-symbols-outlined">download</span>
                        Download Latest Release
                    </button>
                    <a href="@_mod.GitHubRepoUrl" target="_blank" class="w-full flex items-center justify-center gap-3 bg-transparent border border-border-dark hover:bg-border-dark py-3 rounded-lg font-bold text-[#92a4c9] hover:text-white transition-all text-center">
                        <span class="material-symbols-outlined">visibility</span>
                        View on GitHub
                    </a>
                </div>

                <div class="bg-card-dark border border-border-dark rounded-xl p-6">
                    <h3 class="text-sm font-bold uppercase tracking-wider text-[#92a4c9] mb-4">Project Metadata</h3>
                    <div class="grid grid-cols-2 gap-4">
                        <div class="bg-background-dark p-3 rounded-lg border border-border-dark/50">
                            <div class="flex items-center gap-2 text-[#92a4c9] mb-1">
                                <span class="material-symbols-outlined text-sm">star</span>
                                <span class="text-xs font-medium">Stars</span>
                            </div>
                            <p class="text-lg font-bold">@FormatCount(_mod.Stars)</p>
                        </div>
                        <div class="bg-background-dark p-3 rounded-lg border border-border-dark/50">
                            <div class="flex items-center gap-2 text-[#92a4c9] mb-1">
                                <span class="material-symbols-outlined text-sm">fork_right</span>
                                <span class="text-xs font-medium">Forks</span>
                            </div>
                            <p class="text-lg font-bold">@FormatCount(_mod.Forks)</p>
                        </div>
                    </div>
                </div>

                <div class="bg-card-dark border border-border-dark rounded-xl p-6">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-sm font-bold uppercase tracking-wider text-[#92a4c9]">Versions</h3>
                        <a class="text-primary text-xs font-bold hover:underline" href="#">View All</a>
                    </div>
                    <div class="space-y-4">
                        @foreach (var release in _mod.Releases.OrderByDescending(r => r.ReleasedAt))
                        {
                            <div class="flex items-center justify-between group cursor-pointer">
                                <div class="flex items-center gap-3">
                                    <div class="size-2 rounded-full @(release == _mod.Releases.OrderByDescending(r => r.ReleasedAt).First() ? "bg-green-500" : "bg-border-dark")"></div>
                                    <div>
                                        <p class="text-sm font-bold group-hover:text-primary transition-colors">
                                            @release.Version 
                                            @if (release == _mod.Releases.OrderByDescending(r => r.ReleasedAt).First())
                                            {
                                                <span class="text-[10px] ml-2 font-normal px-1.5 py-0.5 bg-primary/20 text-primary rounded">LATEST</span>
                                            }
                                        </p>
                                        <p class="text-xs text-[#92a4c9]">Released @TimeAgo(release.ReleasedAt)</p>
                                    </div>
                                </div>
                                <span class="material-symbols-outlined text-[#92a4c9] group-hover:text-white text-xl">download</span>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </main>
}

@code {
    [Parameter] public int Id { get; set; }
    private Mod? _mod;

    protected override void OnInitialized()
    {
        // Mock data for specific Id
        _mod = new Mod
        {
            Id = Id,
            Name = "Advanced Shader Overhaul",
            Stars = 1245,
            Forks = 284,
            GitHubRepoUrl = "https://github.com/devartist/shader-mod",
            Developer = new Developer { 
                DisplayName = "DevArtist", 
                GitHubUsername = "devartist",
                AvatarUrl = "https://lh3.googleusercontent.com/aida-public/AB6AXuDnqSx65Xl1AiwOzfOubiJubLOtzH5HlWa3E8eyF7zxYqmY81qSNqhYX9cSIobdEYGBs4oh6xZbias0ymnqxmTnOvzrXFnbVejsA0-j532BHZlu0RuO-vlggh7SKE5mENXc6orVtR0i3qWhkNAND80MVxOLwT00YEAORSSUp4ZK_-fOdUD3bBVMCvAfgAeVQjzXIogqNbWKAi5U9HC8bPD9ftC5OJ-WU3xIRBlVDSYzH9Gj3HtSVtnSK9VAXzjaT5kxW1JPL65HkYJh" 
            },
            LongDescription = @"
                <h1>Advanced Shader Overhaul</h1>
                <p>This mod provides real-time ray-tracing capabilities to the engine using custom Vulkan wrappers. It is fully open-source and decentralized, leveraging IPFS for asset distribution.</p>
                <h2>✨ Key Features</h2>
                <ul>
                <li>Dynamic global illumination with zero performance overhead on RTX 30-series+.</li>
                <li>Custom post-processing pipeline for cinematic depth of field.</li>
                <li>Native Vulkan 1.3 support with modular plugin architecture.</li>
                <li>Decentralized distribution via OpenMods registry.</li>
                </ul>
                <h2>🛠 Installation</h2>
                <ol class='list-decimal pl-6 mb-4'>
                <li>Download the latest release from the sidebar.</li>
                <li>Extract the archive to your <code>/plugins</code> folder.</li>
                <li>Run the <code>install.sh</code> or <code>install.bat</code> script to link dependencies.</li>
                <li>Restart the engine and enable 'Advanced Shaders' in settings.</li>
                </ol>
                <blockquote>
                                Note: This mod is currently in Beta. Ensure you backup your world files before applying shader modifications.
                            </blockquote>
            ",
            Releases = new List<Release>
            {
                new Release { Version = "v1.2.4", ReleasedAt = DateTime.UtcNow.AddDays(-2) },
                new Release { Version = "v1.2.3", ReleasedAt = DateTime.UtcNow.AddDays(-120) },
                new Release { Version = "v1.2.0", ReleasedAt = DateTime.UtcNow.AddDays(-150) }
            }
        };
    }

    private string LatestVersion => _mod?.Releases.MaxBy(r => r.ReleasedAt)?.Version ?? "v0.0.0";

    private string FormatCount(int count) => count >= 1000 ? $"{(count / 1000.0):F1}k" : count.ToString();

    private string TimeAgo(DateTime dateTime)
    {
        var span = DateTime.UtcNow - dateTime;
        if (span.TotalDays > 30) return dateTime.ToString("MMM dd, yyyy");
        if (span.TotalDays > 1) return $"{(int)span.TotalDays} days ago";
        if (span.TotalHours > 1) return $"{(int)span.TotalHours} hours ago";
        return "recently";
    }
}
